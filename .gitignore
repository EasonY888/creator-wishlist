node_modules/
dist/
.env
.env.local
*.log

# Next's build output. Nothing here is source, and it is regenerated by `next build`.
.next/

# Prisma 7 generates the client into the source tree; it is a build artifact.
src/generated/
.tools/
reports/screenshots/

# Screen recordings and rendered video. These ship with the submission upload, not
# in history. The two raw captures alone are ~257 MB, and a binary that size is
# painful to remove once it is committed -- git keeps it in every future clone.
pitch/*.mp4
