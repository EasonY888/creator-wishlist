node_modules/
dist/
.env
.env.local
*.log

# Next's build output. Nothing here is source, and it is regenerated by `next build`.
.next/

# Prisma 7 generates the client into the source tree; it is a build artifact.
src/generated/
.tools/
reports/screenshots/
